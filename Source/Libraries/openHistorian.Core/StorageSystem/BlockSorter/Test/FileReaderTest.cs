using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Historian.StorageSystem.BlockSorter.Test
{
    class FileReaderTest
    {
        public static void Test()
        {
            //FileReader file = new FileReader();
            //for (int x = 0; x<1000; x++)
            //{
            //    TimeKey key = new TimeKey(DateTime.Now.AddTicks(x));
            //    file.AddData(key, new byte[] { (byte) x, 1, 2});
            //}
            //System.Windows.Forms.MessageBox.Show("Nodes Created " + file.CountEntries().ToString());

            
        }
    }
}
